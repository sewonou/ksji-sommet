<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                {{ title }}  {% if faq is defined %} : {{ faq.title }} {% endif %}
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-12">
                        {{ form_start(form) }}
                            {{ form_row(form.name) }}
                            {{ form_row(form.address) }}
                            {{ form_row(form.description) }}
                            {{ form_row(form.introduction) }}
                            <div class="col-lg-6">{{ form_row(form.rating) }}</div>
                            <div class="col-lg-6">{{ form_row(form.price) }}</div>

                            <button type="submit" class="btn btn-success">Valider</button>
                            <button type="reset" class="btn btn-default">Annuler</button>
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>